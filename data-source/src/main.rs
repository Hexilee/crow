use proto::curve_service_client::CurveServiceClient;
use proto::{Curve, CurveRequest, RawData, RegisterReply};
use tonic::{Request, Response, Status, Streaming};

fn main() {
    println!("Hello, world!");
}
